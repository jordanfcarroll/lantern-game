using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StaticLayerRenderer : MonoBehaviour {

    // private SpriteRenderer tempRender;

	// // Use this for initialization
	// void Start () {
    //    		tempRender = GetComponent<SpriteRenderer>();
    //    		tempRender.sortingLayerName = "Objects";
	// }
	
	// // Update is called once per frame
	// void Update () {
    //     	tempRender.sortingOrder = (int)Camera.main.WorldToScreenPoint(tempRender.bounds.min).y * -1;

	// }

}
